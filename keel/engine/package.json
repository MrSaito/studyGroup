{
  "name": "@keel/engine",
  "version": "0.1.0",
  "description": "Keel scheduling engine: plan schema, markdown import, elastic projection, consistency score, lapse recovery. Pure TypeScript, zero runtime dependencies.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "verify": "bash verify.sh"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.6.0"
  }
}
